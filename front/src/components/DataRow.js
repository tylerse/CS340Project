export default function DataRow({data, row}){

    const constructDate = () => {
        for(let cell in data){
            
        }
        return 
    }

    return(
        <>
            <tr>
                { constructData }
            </tr>
        </>
    )
}